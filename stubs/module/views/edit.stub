@extends('stackway::layouts.dashboard')

@section('title', 'تعديل {{moduleLabel}}')

@section('header-actions')
    <x-sw-button href="{{ route('{{moduleLower}}.index') }}" variant="secondary" size="sm">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        <span>رجوع للقائمة</span>
    </x-sw-button>
@endsection

@section('content')
<div class="max-w-4xl mx-auto space-y-6">
    <div>
        <h1 class="text-2xl font-bold" style="color: var(--sw-text-primary);">تعديل: {{ ${{moduleCamel}}->name }}</h1>
        <p class="text-sm mt-1" style="color: var(--sw-text-muted);">تحديث بيانات الـ {{moduleLabel}}</p>
    </div>

    <form action="{{ route('{{moduleLower}}.update', ${{moduleCamel}}->id) }}" method="POST" enctype="multipart/form-data">
        @csrf
        @method('PUT')

        <x-sw-card title="البيانات الأساسية" subtitle="تعديل الحقول المراد تحديثها">
            <div class="space-y-4">
                <x-sw-input
                    name="name"
                    label="الاسم"
                    :value="${{moduleCamel}}->name"
                    placeholder="أدخل الاسم..."
                    :required="true"
                />

                <x-sw-select
                    name="status"
                    label="الحالة"
                    :options="['active' => 'نشط', 'inactive' => 'غير نشط', 'pending' => 'معلق']"
                    :selected="${{moduleCamel}}->status"
                    :required="true"
                />

                <x-sw-textarea
                    name="description"
                    label="الوصف"
                    :value="${{moduleCamel}}->description"
                    placeholder="أدخل وصف تفصيلي..."
                    :rows="4"
                />

{{#if:media}}
                <x-sw-file-upload
                    name="image"
                    label="الصورة"
                    :current="${{moduleCamel}}->getFirstMediaUrl('images') ?: null"
                    hint="PNG, JPG, WebP بحد أقصى 5MB"
                />
{{/if:media}}
            </div>

            <x-slot:footer>
                <div class="flex items-center justify-end gap-3">
                    <x-sw-button href="{{ route('{{moduleLower}}.index') }}" variant="secondary">
                        إلغاء
                    </x-sw-button>
                    <x-sw-button type="submit" variant="primary">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                        <span>حفظ التعديلات</span>
                    </x-sw-button>
                </div>
            </x-slot:footer>
        </x-sw-card>
    </form>
</div>
@endsection
